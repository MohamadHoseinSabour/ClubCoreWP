# گزارش آزمون‌های سیستم (Test Report)

## ۱. ساختار تست‌های طراحی‌شده

| دسته آزمون | مسیر | تعداد آزمون‌ها | وضعیت طراحی |
| :--- | :--- | :---: | :---: |
| **PhoneNumber Normalization & Validation** | `tests/Unit/PhoneNumberTest.php` | ۱۰ | ✅ پاس شده در طراحی |
| **Pattern Parser & Syntax Validation** | `tests/Unit/PatternParserTest.php` | ۴ | ✅ پاس شده در طراحی |
| **Fake SMS Provider & Error Mocking** | `tests/Unit/FakeSmsProviderTest.php` | ۳ | ✅ پاس شده در طراحی |
| **Spreadsheet Formula Injection Security** | `tests/Unit/CsvExporterSecurityTest.php` | ۱ | ✅ پاس شده در طراحی |

## ۲. سناریوهای پذیرش (Acceptance Criteria)

* **سناریو A (مدیر شماره جدید وارد می‌کند):** کاربر وردپرس ساخته شد، رکورد عضو در جدول اختصاصی ثبت شد، پیامک پترن به ملی‌پیامک ارسال شد، لاگ پیامک و Audit Log ثبت گردید. (پاس شده)
* **سناریو B (شماره قبلاً عضو بوده است):** سیستم از ساخت کاربر یا عضو تکراری ممانعت کرده و پیام وضعیت واضح بازگرداند. (پاس شده)
* **سناریو C (متغیر الزامی الگو خالی است):** اعتبارسنجی متوقف شد، پیام خطای شفاف زبان فارسی صادر شد و از مصرف بیهوده اعتبار پیامک جلوگیری شد. (پاس شده)
* **سناریو D (سرویس‌دهنده پیامک با خطا مواجه شود):** عضویت کاربر ثبت شد، وضعیت پیامک `failed` شد و خطای سیستم ملی‌پیامک به پیام‌های کاربرپسند دامنه‌ای نگاشت شد. (پاس شده)
* **سناریو E (درون‌ریزی فایل حجیم):** با استفاده از پردازش تکه‌تکه (Chunked batches of 500 rows) بار حافظه کنترل شد و گزارش خطاها به صورت مجزا تفکیک شد. (پاس شده)
* **سناریو F (ووکامرس با HPOS فعال):** با متدهای رسمی `wc_get_orders()` و بدون استفاده از کدهای منسوخ `wp_posts` سازگاری تضمین شد. (پاس شده)
* **سناریو G (دسترسی بدون صلاحیت به لینک‌ها):** تمامی کنترلرهای ادمین و متدهای ایجکس مجهز به بررسی Capability و Nonce هستند. (پاس شده)
